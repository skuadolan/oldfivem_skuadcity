exec ./resources/vMenu/config/permission.cfg

# You probably don't want to change these!
# Only change them if you're using a server with multiple network interfaces.
endpoint_add_tcp "0.0.0.0:30120"
endpoint_add_udp "0.0.0.0:30120"

# Set connection MYSQL-async
<<<<<<< Updated upstream:server-data/server.txt
set mysql_connection_string "server=localhost;database=es_extended;userid=root" #Offline
=======
set mysql_connection_string "server=localhost;database=skuadcity;userid=skuad;password=__skuad021" #Online
#set mysql_connection_string "server=localhost;database=skuadcity;userid=root" #Offline
>>>>>>> Stashed changes:server-data/server.cfg
 
# Disable Couchdb from essentialmode
set es_enableCustomData 1

exec permission.cfg

# Give Essentialmode access to commands
add_ace resource.essentialmode command.sets allow
add_ace resource.essentialmode command.add_principal allow
add_ace resource.essentialmode command.add_ace allow

# Give es_extended access to commands
add_principal group.admin group.user
add_ace resource.es_extended command.add_ace allow
add_ace resource.es_extended command.add_principal allow
add_ace resource.es_extended command.remove_principal allow
add_ace resource.es_extended command.stop allow

# Mysql/Base
start mysql-async
start essentialmode
start es_admin2
start esplugin_mysql

# Resources
setr esx_MenuAlign "top-left"
exec resources.cfg

#Fix Errors
restart sessionmanager

# This allows players to use scripthook based plugins such as lambda menu.
# Set this to 0 to disallow scripthook.
sv_scriptHookAllowed 0

# Uncomment this to enable RCON. Make sure to change the password.
#rcon_password changeme

# A comma-separated list of tags for your server.
# For example:
# - sets tags "drifting, cars, racing"
# Or:
# - sets tags "roleplay, military, tanks"
sets tags "roleplay indonesia, rp indonesia ,skuad , skuadolan, skuad city, roleplay"

# A valid locale identifier for your server's primary language.
# For example "en-US", "fr-CA", "nl-NL", "de-DE", "en-GB", "pt-BR"
sets locale "root-AQ", "en-US", "id-IDN"
# please DO replace root-AQ on the line ABOVE with a real language! :)

# Set an optional server info and connecting banner image url.
# Size doesn't matter, any banner sized image will be fine.
#sets banner_detail "http://url.to/image.png"
#sets banner_connecting "http://url.to/image.png"

# Set your server's hostname
sv_hostname "SKUAD CITY"

# Nested configs!
#exec server_internal.cfg

# Loading a server icon (96x96 PNG file)
load_server_icon skd.png

# convars which can be used in scripts
set temp_convar "hey world!"

# Uncomment this line if you do not want your server to be listed in the server browser.
sv_master1 ""

# Want to only allow players authenticated with a third-party provider like Steam?
# Don't forget Social Club is a third party provider too!
#sv_authMaxVariance 1
#sv_authMinTrust 5

# Add system admins
add_ace group.admin command allow # allow all commands
add_ace group.admin command.quit deny # but don't allow quit
add_principal identifier.steam:110000134a401f0 group.admin # add the admin to the group

# Hide player endpoints in external log output.
sv_endpointprivacy true

# Server player slot limit (must be between 1 and 32)
sv_maxclients 5

# Steam Web API key, if you want to use Steam authentication (https://steamcommunity.com/dev/apikey)
# -> replace "" with the key
set steam_webApiKey ""

# License key for your server (https://keymaster.fivem.net)
<<<<<<< Updated upstream:server-data/server.txt
sv_licenseKey "s3iw4nierdwt4izyh9wolsbhd4e17z1f" #Offline
=======
sv_licenseKey "ezziczdbvcssf6dobilf8c1zcsssr9pb" #Online
#sv_licenseKey "s3iw4nierdwt4izyh9wolsbhd4e17z1f" #Offline
>>>>>>> Stashed changes:server-data/server.cfg
